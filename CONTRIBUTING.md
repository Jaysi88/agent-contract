# Contributing

PRs that add slogans without a scanner will be closed.

1. Every skill is a folder under `skills/<name>/` with `SKILL.md` + `scripts/`.
2. Examples under `examples/` must fail the scanner (exit 1) or the CI is lying.
3. Do not print secrets. Do not expand scope in the skill itself.
